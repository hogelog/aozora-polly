require "spec_helper"

describe Aozora::Ssml do
  it "has a version number" do
    expect(Aozora::Ssml::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
